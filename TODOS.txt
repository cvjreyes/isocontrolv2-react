DONE ✔
- FEEDPipes.jsx requests with axios or custom api ( recommended ) with included error handling and request state like isLoading
- TABLE make wrapper
- TABLE make routing
- TABLE get info
- TABLE display info
- TABLE implement table
- TABLE modify table
- TABLE paste from excel
- TABLE filter then paste
- TABLE paste more than 1 line
- TABLE paste more than 1 line on filtered
- TABLE copy 1 row
- TABLE copy +1 row
- TABLE copy all rows
- TABLE paste 1 cell
- TABLE paste +1 cell ❌
- TABLE paste 1 row
- TABLE paste +1 row
- TABLE - when change something => update line_ref + tag
- TABLE copy row on click ID
- HIGHLIGHT CHANGES state array saving objects with id
- HIGHLIGHT CHANGES in render => check for id
- HIGHLIGHT CHANGES if match: greenish background
- HIGHLIGHT CHANGES this way when saving you only have to go throgh those specific ids + keys
- TABLE add dropdowns to: line_ref, area, diameter, train, status
- TABLE submit changes 
- TABLE delete row 
- si already exists cambiar color alerta
- añadir logo isocontrol
- ADDING ROW
- que se vea el owner

TODO ❌
- pass progress to feed through url state OR params NOT localStorage
- añadir 'copied' al clicar copiar
- mover sidebar arriba
- añadir piecharts

- Crear select de Roles ( Feed: Speciality Lead || IFD: Design)

- change status "MODELLED (100%)" to "modelled" then format on client and progress values on separate file in node

Edit Forecast: cuando se añade que no se muevan los elementos
- usar overflow hidden y fixed height

- change password

- IFD:
  - Main:
    - add entries?

  - Tabla My tray (Bandeja donde completar las acciones)
    - Boton Next Step (Pasar a la siguiente bandeja)
    - Boton Unclaim (Para vuelva a la bandeja en la que estaba)
    - Boton Return (Para que vaya a la bandeja anterior en la que estaba)
    - Boton Delete (Para borrarla definitivamente)

    - Columna Actions
      - V (Llevarlo a la tabla de V, Valvulas)
      - I (Llevarlo a la tabla de I, Instrumentacion)
      - N/A 

  - Tabla de Modelled (Claim a MyTray)
  - Tabla de S-Stress (Claim a MyTray)
  - Tabla de R-Stress (Claim a MyTray)
  - Tabla de Stress (Claim a MyTray)
  - Tabla de Supports (Claim a MyTray)
  - Tabla de S-Design (Claim a MyTray)

  - Tabla Trash ( Para restaurar los modelled que hayas eliminado)
  - Tabla Hold
    - Boton Hold (Abre un modal donde puedes ver la descripcion de los holds, puedes añadir y guardar descripciones )

PARA QUE SALGA MODELLED:
- se importa un csv de 3d
- este csv rellena dpipes
- si la línea ya existe => MODELLED
- si la línea no existía => MODELLED*

OLD
- se importa el csv
- del csv se actualiza tabla dpipes ( CRON )
- de dpipes se comprueba con ifd_pipes 
- se decide si modelled o modelled*
- se pasa a otra tabla

NEW
- attempt to merge csv import with ifd_pipes => comprobar el code in prod ( node_cron de fmc )
(NODE-CRON)[https://github.com/cvjreyes/node-cron/blob/main/app/src/resources/cron/cron.controller.js]
- re-write node-cron for it to update ifd_pipes or fallback to use ifd_pipes_aux
- if estimated => modelled
- if _ => modelled*

OTHER
  - add error handler for when server is down ( something like 'please hold on' ) {
    - mientras se actualiza lista de líneas
    - encontrar solución para que la gente no pueda cambiar cosas
  }
  - tabla escondida en FEED y en IFD => bring it back pipingProgress.js en old

DUDAS JORGE:
- PREGUNTAR CÁLCULO IFD WEIGHT + aplicar
- preguntar sequential número de ceros

FEED:
  - when paste => get values from line_ref

TABLES que no llegué a probar:
- react-virtualized
- react-data-grid
- react-super-responsive-table
- react-bootstrap-table
- react-datasheet
- react-grid
